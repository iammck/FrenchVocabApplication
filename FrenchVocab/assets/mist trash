//		protected void populateDatabase() {
//			new AsyncTask<String,String,String>(){
//				@Override
//				protected String doInBackground(String... chapters) {
//					// for each title in chapters
//					for(int x = 1 ; x <= chapters.length ; x++ ){
//						String testFile  = getChapterFromFile(chapters[x-1]);
//						insertChapStringIntoDb(x,testFile);
//						Log.v(TAG, "populateDatabase() " + x +" of " + chapters.length + "chapters written to db");		
//						
//					}
//					Log.v(TAG, "populateDatabase() completed in background using CONFLICT_IGNORE");		
//					return "ok";
//				}
//
//				@Override
//				protected void onPostExecute(String result) {
//					super.onPostExecute(result);
//					dialog.dismiss();
//					((VocabListActivity)context).restartVocabList();
//				}
//
//				@Override
//				protected void onPreExecute() {
//					super.onPreExecute();
//					dialog = new ProgressDialog(context, ProgressDialog.STYLE_SPINNER);
//					dialog.setTitle(R.string.loading_database);
//					dialog.show();
//				}
//				
//			}.execute(VocabListActivity.AVAILABLE_CHAPTERS);
//			
			
//	//		String testFile  = getChapterFromFile("chapter4");
//	//		insertChapter(1,testFile);
//	//		Log.v(TAG, "populateDatabase() completed using CONFLICT_IGNORE");		

//	}
		
//		ProgressDialog dialog;
//		public void putChapterInDatabase(String chapter, int chapterNumber){
//			String chapterText  = getChapterFromFile(chapter);
//			putChapterInDataBase(chapterText,chapterNumber);
//			Log.v(TAG, "putChapterInDatabase() finished putting " + chapter);
//			
			
//			new AsyncTask<String,String,String>(){
//				@Override
//				protected String doInBackground(String... chapters) {
//					// for each title in chapters
//					for(int x = 1 ; x <= chapters.length ; x++ ){
//						String testFile  = getChapterFromFile(chapters[x-1]);
//						insertChapStringIntoDbWithChapNumber(x,testFile);
//						Log.v(TAG, "populateDatabase() " + x +" of " + chapters.length + "chapters written to db");		
//						
//					}
//					Log.v(TAG, "populateDatabase() completed in background using CONFLICT_IGNORE");		
//					return "ok";
//				}
//
//				@Override
//				protected void onPostExecute(String result) {
//					super.onPostExecute(result);
//					dialog.dismiss();
//					((VocabListActivity)context).restartVocabList();
//				}
//
//				@Override
//				protected void onPreExecute() {
//					super.onPreExecute();
//					dialog = new ProgressDialog(context, ProgressDialog.STYLE_SPINNER);
//					dialog.setTitle(R.string.loading_database);
//					dialog.show();
//				}
//				
//			}.execute(chapter);
			
//		}



/////////////////
/////////////////
/////////////////
